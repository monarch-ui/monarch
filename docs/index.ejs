
<% function excerpt(content) {
  if(content.indexOf("<!-- more -->") == -1) return content;
  else return content.split("<!-- more -->")[0]; 
} %>

<% for(var slug in public.data._data){ var post = public.data._data[slug]; %>
  <section class='<%- slug %>'>
    <%- excerpt(partial("data/" + slug)) %>
  </section>
<% }; %>